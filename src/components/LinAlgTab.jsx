import { useEffect, useMemo, useRef, useState } from 'react';
import { analyzeMatrix, formatEigenLabel, eigenvectorLineEndpoints, gaussJordan2x2, luTrace2x2, eigenTrace2x2 } from '../lib/linalg';
import { paramNumber, readHashState, updateHashState } from '../lib/urlState';
import { matrixPythonCode, matrixSympyCode, matrixMatlabCode } from '../lib/codegen';
import { buildAssignmentReport, buildLatexReport, downloadText, exportSvgElement, exportSvgAsPng, printReport } from '../lib/download';
const VIEW_SIZE=360;const fmt=n=>Number.isFinite(n)?n.toFixed(3).replace(/\.000$/,''):'—';const fmtRow=r=>r.map(fmt).join('   ');function toScreen(x,y,l){const s=(VIEW_SIZE/2-20)/l;return[VIEW_SIZE/2+x*s,VIEW_SIZE/2-y*s]}
export default function LinAlgTab(){const initial=useMemo(()=>readHashState().params,[]),plotRef=useRef(null);const[m11,setM11]=useState(()=>paramNumber(initial,'a',2)),[m12,setM12]=useState(()=>paramNumber(initial,'b',1)),[m21,setM21]=useState(()=>paramNumber(initial,'c',1)),[m22,setM22]=useState(()=>paramNumber(initial,'d',2)),[vx,setVx]=useState(()=>paramNumber(initial,'vx',1)),[vy,setVy]=useState(()=>paramNumber(initial,'vy',0)),[mode,setMode]=useState('gauss'),[step,setStep]=useState(0);useEffect(()=>updateHashState('matrix',{a:m11,b:m12,c:m21,d:m22,vx,vy}),[m11,m12,m21,m22,vx,vy]);const result=useMemo(()=>analyzeMatrix(m11,m12,m21,m22,vx,vy),[m11,m12,m21,m22,vx,vy]),gauss=useMemo(()=>gaussJordan2x2(m11,m12,m21,m22),[m11,m12,m21,m22]),lu=useMemo(()=>luTrace2x2(m11,m12,m21,m22),[m11,m12,m21,m22]),eig=useMemo(()=>eigenTrace2x2(m11,m12,m21,m22),[m11,m12,m21,m22]);useEffect(()=>setStep(0),[m11,m12,m21,m22,mode]);const traces={gauss:gauss.steps,lu:lu.steps,eigen:eig.steps},steps=traces[mode],cur=steps[Math.min(step,steps.length-1)];const{v,vTransformed,eigenInfos,axisLimit,determinant,trace,invertible,inverse,frobeniusNorm}=result,[ox,oy]=toScreen(0,0,axisLimit),[vxS,vyS]=toScreen(v[0],v[1],axisLimit),[tx,ty]=toScreen(vTransformed[0],vTransformed[1],axisLimit);const presets={identity:[1,0,0,1],rotation:[0,-1,1,0],shear:[1,1,0,1],stretch:[2,0,0,.5],reflection:[1,0,0,-1],projection:[1,0,0,0],jordan:[2,1,0,2]};const apply=p=>{const[a,b,c,d]=presets[p];setM11(a);setM12(b);setM21(c);setM22(d)};const config={m11,m12,m21,m22,vx,vy};const report={title:'SimuMath Matrix Laboratuvar Raporu',summary:'2x2 lineer dönüşümün sayısal ve geometrik analizi.',parameters:{A:`[[${m11}, ${m12}], [${m21}, ${m22}]]`,v:`[${vx}, ${vy}]`},results:{determinant:fmt(determinant),trace:fmt(trace),frobeniusNorm:fmt(frobeniusNorm),invertible:invertible?'evet':'hayır'},notes:['Gauss-Jordan, LU ve özdeğer/özvektör adımları eğitim amaçlı izlenebilir.']};return <div className="tab-panel"><div className="control-panel"><h2>Matrix Presets & Gallery</h2><p className="description">Standart lineer cebir dönüşümlerini yükle ve çözüm algoritmalarını adım adım izle.</p><div className="btn-row" style={{flexWrap:'wrap'}}>{Object.keys(presets).map(p=><button key={p} className="btn btn-secondary" onClick={()=>apply(p)}>{p}</button>)}</div><div className="matrix-grid"><span className="bracket">A=[</span><input type="number" value={m11} onChange={e=>setM11(+e.target.value)}/><input type="number" value={m12} onChange={e=>setM12(+e.target.value)}/><span/><span/><input type="number" value={m21} onChange={e=>setM21(+e.target.value)}/><input type="number" value={m22} onChange={e=>setM22(+e.target.value)}/><span className="bracket">]</span></div><div className="field"><label>vx</label><input type="number" value={vx} onChange={e=>setVx(+e.target.value)}/></div><div className="field"><label>vy</label><input type="number" value={vy} onChange={e=>setVy(+e.target.value)}/></div><div className="result-banner">det={fmt(determinant)} · tr={fmt(trace)} · ‖A‖F={fmt(frobeniusNorm)}<br/>{eigenInfos.map((x,i)=>formatEigenLabel(x,i+1)).join(' · ')}</div><div className="btn-row" style={{flexWrap:'wrap'}}><button className="btn btn-secondary" onClick={()=>downloadText('simumath-matrix.py',matrixPythonCode(config))}>NumPy</button><button className="btn btn-secondary" onClick={()=>downloadText('simumath-matrix-sympy.py',matrixSympyCode(config))}>SymPy</button><button className="btn btn-secondary" onClick={()=>downloadText('simumath-matrix.m',matrixMatlabCode(config))}>MATLAB</button><button className="btn btn-secondary" onClick={()=>downloadText('simumath-matrix-report.tex',buildLatexReport(report))}>LaTeX</button><button className="btn btn-secondary" onClick={()=>printReport(report)}>PDF</button><button className="btn btn-secondary" onClick={()=>exportSvgElement(plotRef.current,'simumath-matrix.svg')}>SVG</button><button className="btn btn-secondary" onClick={()=>exportSvgAsPng(plotRef.current,'simumath-matrix-3x.png',3)}>PNG</button></div></div><div style={{display:'grid',gap:20}}><div className="viz-panel"><div className="btn-row"><button className={`btn ${mode==='gauss'?'btn-primary':'btn-secondary'}`} onClick={()=>setMode('gauss')}>Gauss-Jordan</button><button className={`btn ${mode==='lu'?'btn-primary':'btn-secondary'}`} onClick={()=>setMode('lu')}>LU</button><button className={`btn ${mode==='eigen'?'btn-primary':'btn-secondary'}`} onClick={()=>setMode('eigen')}>Eigen</button></div><div className="viz-title">Adım {step+1}/{steps.length}</div><div className="result-banner">{cur.label}</div>{cur.matrix&&<div className="progress-mono" style={{whiteSpace:'pre'}}>{fmtRow(cur.matrix[0])}{'\n'}{fmtRow(cur.matrix[1])}</div>}{cur.L&&<div className="progress-mono" style={{whiteSpace:'pre'}}>L = [{fmtRow(cur.L[0])}]{'\n'}    [{fmtRow(cur.L[1])}]{'\n\n'}U = [{fmtRow(cur.U[0])}]{'\n'}    [{fmtRow(cur.U[1])}]</div>}<div className="btn-row"><button className="btn btn-secondary" disabled={step===0} onClick={()=>setStep(s=>s-1)}>← Geri</button><button className="btn btn-primary" disabled={step>=steps.length-1} onClick={()=>setStep(s=>s+1)}>İleri →</button></div></div><div className="viz-panel"><div className="viz-title">Vektör Dönüşümü ve Özvektörler</div><div className="viz-body"><svg ref={plotRef} viewBox={`0 0 ${VIEW_SIZE} ${VIEW_SIZE}`} width="100%" style={{maxWidth:420}}><line x1="0" y1={VIEW_SIZE/2} x2={VIEW_SIZE} y2={VIEW_SIZE/2} stroke="#263758"/><line x1={VIEW_SIZE/2} y1="0" x2={VIEW_SIZE/2} y2={VIEW_SIZE} stroke="#263758"/>{eigenInfos.map((info,i)=>{if(!info.isReal||!info.vector)return null;const[p1,p2]=eigenvectorLineEndpoints(info.vector,axisLimit),[x1,y1]=toScreen(p1[0],p1[1],axisLimit),[x2,y2]=toScreen(p2[0],p2[1],axisLimit);return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#e65b5b" strokeDasharray="6 4"/>})}<line x1={ox} y1={oy} x2={vxS} y2={vyS} stroke="#4d8cf2" strokeWidth="3"/><line x1={ox} y1={oy} x2={tx} y2={ty} stroke="#3ecf8e" strokeWidth="3"/></svg></div>{inverse&&<div className="progress-mono">A⁻¹ = [[{fmt(inverse[0][0])}, {fmt(inverse[0][1])}], [{fmt(inverse[1][0])}, {fmt(inverse[1][1])}]]</div>}</div></div></div>}
